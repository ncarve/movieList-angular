import { GenreColorDirective } from './genre-color.directive';

describe('GenreColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GenreColorDirective(null);
    expect(directive).toBeTruthy();
  });
});
